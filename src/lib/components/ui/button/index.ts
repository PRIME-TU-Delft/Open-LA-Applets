import Action from './action-button.svelte';
import Root, {
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  buttonVariants
} from './button.svelte';

export {
  Action,
  //
  Root as Button,
  buttonVariants,
  Root,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
  type ButtonProps as Props
};
