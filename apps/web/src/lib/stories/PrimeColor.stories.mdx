import { Meta, ColorPalette, ColorItem } from '@storybook/blocks';
import { PrimeColor, BackgroundColors } from 'utils/PrimeColors';

<Meta title="Colors" />

<ColorPalette>
  <ColorItem title="Background colors" subtitle="Some of the greys" colors={BackgroundColors} />
  <ColorItem title="Prime colors" subtitle="Enum of colors" colors={PrimeColor} />
</ColorPalette>

Colors are extended from the official [Corporate Identity colors](https://www.tudelft.nl/en/tu-delft-corporate-design/components/colour).
Colors are defined in the `utils/PrimeColors.ts` file.
