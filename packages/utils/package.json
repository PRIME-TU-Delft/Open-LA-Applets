{
  "name": "utils",
  "version": "0.0.0",
  "description": "Utilities for all packages",
  "type": "module",
  "module": "index.ts",
  "svelte": "index.ts",
  "license": "MIT",
  "scripts": {
    "lint": "prettier --check --ignore-path=../../.prettierignore . && eslint \".\"",
    "format": "prettier --write --ignore-path=../../.prettierignore ."
  },
  "devDependencies": {
    "eslint-config-custom": "*"
  },
  "dependencies": {
    "@types/three": "^0.146.0",
    "three": "^0.147.0"
  }
}
